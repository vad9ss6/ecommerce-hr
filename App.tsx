import { StatusBar } from "expo-status-bar";
import "@/global.css";

export default function App() {
  return (
    <div className="flex-1 items-center justify-center bg-background-light">
      <StatusBar style="auto" />
    </div>
  );
}
